// Inicializamos una variable con un dato de tipo cadena
var variable = "Audifonos Gamer";
var disponible;

// En javascript, podemos reasignar una variable ya inicializada
variable = true;
disponible = "Nada"

// Mostramos dentro de la consola las variables que habiamos asignado antes
console.log(variable);
console.log(disponible);